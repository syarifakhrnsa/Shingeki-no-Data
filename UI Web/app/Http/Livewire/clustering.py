import numpy as np
from sklearn.cluster import KMeans
import json
import sys
import pandas as pd

# day = sys.argv[1]
# locations = sys.argv[2]

# x = pd.DataFrame(json.loads(locations))

# km = KMeans(n_clusters=3)
# x["cluster"] = km.fit_predict(x)
# output_array = x.values.tolist()
# json_output = json.dumps(output_array)

x = 3
print(x)

